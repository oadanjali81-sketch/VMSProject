@*
    AddVisitor.cshtml — REDIRECTED

    This standalone page duplicated the Add Visitor modal already present in
    VisitorManagement.cshtml AND contained its own "new bootstrap.Modal(...)"
    call that conflicted with the global modal manager.

    Visitor registration is handled exclusively via the modal overlay on
    the Visitor Management page. Any direct GET to /User/AddVisitor
    is silently redirected there.
*@
@{
    Layout = null;
    Context.Response.Redirect("/User/VisitorManagement", permanent: true);
}
